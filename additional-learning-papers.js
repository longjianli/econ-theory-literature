window.ADDITIONAL_LEARNING_PAPERS = [
  {
    id: "ba-2026-robust-misspecified-models",
    title: "Robust Misspecified Models",
    authors: "Cuimin Ba",
    journal: "American Economic Review",
    year: 2026,
    sourceType: "Published article",
    doi: "10.1257/aer.20240246",
    url: "https://doi.org/10.1257/aer.20240246",
    status: "Digested",
    tags: ["misspecified-learning", "behavioral-learning", "learning", "robust-theory", "top5-te"],
    takeaway: "Studies when misspecified models persist even when agents compare them with alternatives, including the correct model. Persistence depends on predictive accuracy along the observed path and on prior tightness around the equilibrium pattern of data.",
    digest: {
      short: "Ba studies robustness of misspecified models when agents can switch paradigms. The key point is subtle: a false model can survive infinite data if it predicts the equilibrium data well enough and if the prior around that model is tight. This gives a learning foundation for persistent systemic biases and paradigm lock-in.",
      motivation: `
        <p><strong>Motivation.</strong> People often do not just update parameters inside one model; they may also compare models. Yet bad models can persist for a long time in politics, organizations, finance, and personal decision making.</p>
        <p><strong>Reality example.</strong> A manager may believe worker effort is the only driver of output. Even after observing many outcomes, she may not switch to a richer model with demand shocks or team spillovers if her simple model predicts the data she happens to observe and she has a tight prior around it.</p>
        <p><strong>Implication.</strong> The paper explains why correctness alone does not guarantee survival. Simpler or more entrenched wrong models can be more robust than the true model.</p>
      `,
      medium: `
        <p><strong>Big idea.</strong> Standard misspecified learning fixes the subjective model class and asks which pseudo-true parameter survives. Ba adds model competition: the agent may switch from one model to another when the alternative fits the observed data sufficiently better.</p>
        <p><strong>Main result in words.</strong> Model persistence depends on asymptotic accuracy along the equilibrium path and the tightness of the prior around that path. A misspecified model can persist against many alternatives, even the correct model, if it is good enough at predicting the endogenous data the agent sees.</p>
      `,
      deep: `
        <h3>1. Environment</h3>
        <p>The agent has a current model \(M\) and possible alternative models \(M'\). A model specifies beliefs over outcome distributions conditional on actions. The agent chooses actions, observes outcomes, and evaluates model fit using the data generated by her own behavior.</p>
        <div class="math">
          \\[
            y_t\\sim p(\\cdot\\mid a_t),
            \\qquad
            M: y_t\\sim q_M(\\cdot\\mid a_t,\\theta_M).
          \\]
        </div>
        <h3>2. Model Switching</h3>
        <p>The agent does not continuously switch to every marginally better model. Switching is sticky: an alternative must fit the data sufficiently better than the current model.</p>
        <div class="math">
          \\[
            \\text{switch from }M\\text{ to }M'
            \\quad\\Longleftrightarrow\\quad
            \\text{Fit}(M')-\\text{Fit}(M)\\ge \\kappa.
          \\]
        </div>
        <h3>3. Main Result</h3>
        <div class="theorem-box">
          <h3>Robust Model Persistence</h3>
          <p>A model is robust if it persists against a broad set of alternatives. Robustness is characterized by two objects: the model's asymptotic predictive accuracy along the equilibrium data path and the tightness of the prior around the equilibrium predictions.</p>
        </div>
      `,
      example: `
        <h3>Overconfidence Example</h3>
        <p>A worker chooses effort \(a\), output is \(y=a+\\varepsilon\), but the worker overestimates ability. A correct model attributes some failures to low ability or shocks; an overconfidence model attributes failures to bad luck and keeps effort incentives high.</p>
        <div class="math">
          \\[
            q_{over}(y\\mid a)
            \\approx
            p(y\\mid a^*_{over})
            \\quad\\text{along the observed path}.
          \\]
        </div>
        <p>If the overconfidence model predicts the data generated by overconfident behavior well enough, and its prior is tight, the agent may not switch even when the correct model is available.</p>
      `,
      technical: `
        <p><strong>Contribution to literature.</strong> The paper extends Berk-Nash/misspecified-learning logic from parameter learning inside one model to competition among models. It is closely related to Esponda-Pouzo, Fudenberg-Lanzani-Strack, Heidhues-Koszegi-Strack, Frick-Iijima-Ishii, and Bohren-Hauser.</p>
        <p><strong>Proof technique.</strong> The proof compares long-run likelihood performance across models along the endogenous data path. The key is that model fit is evaluated on data generated by the model-induced behavior, not on all possible data.</p>
        <p><strong>Useful secondary technique.</strong> Separate two forces: asymptotic accuracy and prior tightness. This is useful for any project where model simplicity or ideology competes with statistical fit.</p>
        <p><strong>Open questions.</strong> Natural extensions include social model switching, strategic persuasion of model choice, platforms that choose which model-comparison data users see, and welfare criteria when the correct model is available but not adopted.</p>
      `,
    },
  },
];

(function addAdditionalLearningPapers() {
  if (!Array.isArray(window.PAPERS) && typeof PAPERS === "undefined") return;
  const list = Array.isArray(window.PAPERS) ? window.PAPERS : PAPERS;
  const ids = new Set(list.map((paper) => paper.id));
  for (const paper of window.ADDITIONAL_LEARNING_PAPERS) {
    if (!ids.has(paper.id)) list.push(paper);
  }
})();
